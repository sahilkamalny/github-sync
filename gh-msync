#!/bin/bash
# GitHub CLI extension entrypoint.
# This allows `gh msync` when installed via `gh extension install`.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/scripts/gh-msync" "$@"
